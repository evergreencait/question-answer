<li>
  {{question.inquiry}} by {{question.author}}
  {{question.notes}}
  {{#if isImageShowing }}
    <p><img scr={{question.image}} {{action 'imageHide'}}></p>
  {{else}}
    <button {{action 'imageShow'}}>Show image</button>
  {{/if}}
</li>
